-- Remove columns from units table
ALTER TABLE public.units
  DROP COLUMN IF EXISTS category,
  DROP COLUMN IF EXISTS property_type,
  DROP COLUMN IF EXISTS street_address,
  DROP COLUMN IF EXISTS city,
  DROP COLUMN IF EXISTS state,
  DROP COLUMN IF EXISTS zipcode,
  DROP COLUMN IF EXISTS county,
  DROP COLUMN IF EXISTS year_built,
  DROP COLUMN IF EXISTS brown_water,
  DROP COLUMN IF EXISTS has_pool,
  DROP COLUMN IF EXISTS lot_size,
  DROP COLUMN IF EXISTS school_score,
  DROP COLUMN IF EXISTS hoa_id,
  DROP COLUMN IF EXISTS subdivision,
  DROP COLUMN IF EXISTS legal_description,
  DROP COLUMN IF EXISTS latitude,
  DROP COLUMN IF EXISTS longitude,
  DROP COLUMN IF EXISTS renovation_date,
  DROP COLUMN IF EXISTS source,
  DROP COLUMN IF EXISTS source_name,
  DROP COLUMN IF EXISTS date_purchased,
  DROP COLUMN IF EXISTS date_sold,
  DROP COLUMN IF EXISTS asset_id_counter,
  DROP COLUMN IF EXISTS asset_id,
  DROP COLUMN IF EXISTS markets_id,
  DROP COLUMN IF EXISTS funds_id,
  DROP COLUMN IF EXISTS portfolios_id,
  DROP COLUMN IF EXISTS deals_id,
  DROP COLUMN IF EXISTS purchase_price,
  DROP COLUMN IF EXISTS uw_arv,
  DROP COLUMN IF EXISTS uw_rent,
  DROP COLUMN IF EXISTS uw_capex,
  DROP COLUMN IF EXISTS cost_basis;